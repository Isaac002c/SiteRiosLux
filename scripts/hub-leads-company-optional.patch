--- a/src/app/api/integrations/rios-lux/leads/route.ts
+++ b/src/app/api/integrations/rios-lux/leads/route.ts
@@ -10,7 +10,7 @@
 const leadSchema = z.object({
   submissionId: z.string().min(20).max(80),
   name: z.string().min(2).max(100),
-  company: z.string().min(2).max(140),
+  company: z.string().min(2).max(140).optional(),
   whatsapp: z.string().min(10).max(30),
   email: z.string().email().max(160),
   eventType: z.string().min(2).max(80),
